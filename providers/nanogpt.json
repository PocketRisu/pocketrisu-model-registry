{
  "id": "nanogpt",
  "name": "NanoGPT",
  "version": 1,
  "description": "Pay-per-use OpenAI-compatible gateway (nano-gpt.com).",
  "messageFormat": "openai",

  "auth": { "type": "bearer", "fieldKey": "api-key" },

  "schema": {
    "type": "object",
    "properties": {
      "api-key": {
        "type": "array",
        "items": { "type": "string" },
        "title": "API Key",
        "description": "NanoGPT API key. Multiple keys rotate automatically."
      },
      "model": {
        "type": "string",
        "title": "Model",
        "description": "NanoGPT model ID. See https://nano-gpt.com/api for the live list.",
        "examples": [
          "claude-sonnet-4.7",
          "claude-opus-4.7",
          "gpt-5.5",
          "gpt-5",
          "gemini-3-pro",
          "deepseek-chat"
        ]
      },
      "custom-params": {
        "type": "string",
        "title": "Custom Params (JSON)",
        "description": "Free-form JSON merged into the request body.",
        "default": ""
      },
      "pricing-input": {
        "type": "number",
        "title": "Pricing — Input ($/1M tokens)",
        "default": 0,
        "minimum": 0
      },
      "pricing-output": {
        "type": "number",
        "title": "Pricing — Output ($/1M tokens)",
        "default": 0,
        "minimum": 0
      },
      "pricing-cache-read": {
        "type": "number",
        "title": "Pricing — Cache Read ($/1M tokens)",
        "default": 0,
        "minimum": 0
      },
      "pricing-cache-write": {
        "type": "number",
        "title": "Pricing — Cache Write ($/1M tokens)",
        "default": 0,
        "minimum": 0
      },
      "temperature": {
        "type": "number",
        "title": "Temperature",
        "default": 1.0,
        "minimum": 0,
        "maximum": 2
      }
    },
    "required": ["api-key", "model"]
  },

  "uiSchema": {
    "api-key": { "ui:widget": "secret-list" },
    "model": { "ui:widget": "combobox" },
    "custom-params": { "ui:widget": "textarea", "ui:options": { "rows": 3, "collapsed": true } },
    "pricing-input": { "ui:widget": "number", "ui:options": { "collapsed": true } },
    "pricing-output": { "ui:widget": "number", "ui:options": { "collapsed": true } },
    "pricing-cache-read": { "ui:widget": "number", "ui:options": { "collapsed": true } },
    "pricing-cache-write": { "ui:widget": "number", "ui:options": { "collapsed": true } },
    "temperature": { "ui:widget": "slider", "ui:options": { "step": 0.1 } }
  },

  "models": [],

  "request": {
    "url": "https://nano-gpt.com/api/v1/chat/completions",
    "method": "POST",
    "headers": {
      "content-type": "application/json"
    },
    "body": {
      "model": "{{model}}",
      "messages": "{{$messages}}",
      "max_tokens": "{{$max_tokens}}",
      "stream": "{{$streaming}}",
      "temperature": "{{temperature}}"
    }
  },

  "response": {
    "streaming": {
      "format": "sse",
      "style": "openai",
      "textPath": "choices[0].delta.content",
      "doneSentinel": "[DONE]",
      "usagePath": "usage"
    },
    "nonStreaming": {
      "textPath": "choices[0].message.content",
      "usagePath": "usage"
    }
  }
}
